<%= form_with(model: user_user_type) do |form| %>
  <% if user_user_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_user_type.errors.count, "error") %> prohibited this user_user_type from being saved:</h2>

      <ul>
        <% user_user_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
