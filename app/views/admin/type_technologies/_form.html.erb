<%= form_with(model: type_technology) do |form| %>
  <% if type_technology.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_technology.errors.count, "error") %> prohibited this type_technology from being saved:</h2>

      <ul>
        <% type_technology.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
