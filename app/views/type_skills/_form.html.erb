<%= form_with(model: type_skill) do |form| %>
  <% if type_skill.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_skill.errors.count, "error") %> prohibited this type_skill from being saved:</h2>

      <ul>
        <% type_skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
